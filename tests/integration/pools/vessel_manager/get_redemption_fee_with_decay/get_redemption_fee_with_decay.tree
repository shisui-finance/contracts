test_get_redemption_fee_with_decay.cairo
├── when base rate is defined
│   └── it should return redemption fee
└── when base rate is defined and redemption fee below asset draw
    └── it should revert with Fee bigger than assert draw