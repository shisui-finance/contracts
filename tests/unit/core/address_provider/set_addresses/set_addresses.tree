test_set_addresses.cairo
├── when the caller is not the owner
│   └── it should revert 
└── when the caller is the owner
    ├── when the addresses has already been set
    │   └── it should revert with AddressProvider__AlreadySet
    ├── when the addresses array length is not equal to 15
    │   └── it should revert with AddressProvider__Expected15Addresses
    ├── when the first address is the zero address
    │   └── it should revert with CommunErrors__AddressZero
    ├── when the last address is the zero address
    │   └── it should revert with CommunErrors__AddressZero
    ├── when at least one of the addresses is the zero address
    │   └── it should revert with CommunErrors__AddressZero
    └── when all addresses are non-zero
        └── it should set all addresses
